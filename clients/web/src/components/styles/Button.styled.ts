import styled from 'styled-components';

const Button = styled.button`
  border-radius: 4px;
  width: 100%;
  font-size: 1em;
  border: none;
  background: #074a91;
  color: #fff;
  padding: 12px;
  margin: 6px 0;
  &:hover {
    cursor: pointer;
  }
`;
export default Button;
